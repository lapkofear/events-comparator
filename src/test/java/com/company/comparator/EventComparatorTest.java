package com.company.comparator;

import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class EventComparatorTest {
    private static final Logger LOG = LoggerFactory.getLogger(EventComparatorTest.class.getName());

    @Test
    public void delayedStreamTest() throws InterruptedException {
//        new EventComparator().findDifferentEntries(null, () -> );

//        entryStream.forEach(eventsDifferenceEntry -> {
//            LOG.error("===>  |EventComparatorTest| |delayedStreamTest| line:5  ");
//        });
//
//        CompletableFuture.runAsync(() -> entryStream.forEach(eventsDifferenceEntry -> {
//            LOG.error("===>  |EventComparatorTest| |delayedStreamTest| line:5  ");
//        }));

//        Thread.sleep(11000);

    }
}